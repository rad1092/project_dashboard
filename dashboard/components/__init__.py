"""재난 앱에서 재사용하는 Streamlit UI 컴포넌트 패키지.

페이지 파일이 제목 블록, 추천 카드, 흐름 카드, 지도 렌더링 코드를
직접 반복하지 않도록 공통 UI 함수들을 묶어 둔 하위 패키지다.
덕분에 페이지는 "무엇을 보여 줄지"에 집중하고, 컴포넌트는 "어떻게 보여 줄지"를 맡는다.
"""

# __init__.py 파일이 있기 때문에 Python 은 이 폴더를 하나의 UI 패키지처럼 import 할 수 있다.
# 이 파일에는 실행 로직을 넣지 않고, 컴포넌트 패키지의 역할 설명만 유지한다.
# 즉, 반복 UI 를 어디에 모아 두었는지 패키지 수준에서 설명하는 표지판에 가깝다.
