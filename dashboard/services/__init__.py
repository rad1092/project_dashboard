"""데이터 로딩, 정규화, 추천 계산을 맡는 서비스 패키지.

페이지 파일이 CSV 경로 탐색, 컬럼 정리, 거리 계산, 추천 규칙을 직접 떠안지 않도록
데이터 책임을 별도 모듈로 분리한 패키지다.
현재는 전처리된 과거 데이터를 읽지만, 나중에 실시간 API 를 붙일 때도 같은 진입점을 유지하는 것이 목표다.
"""

# 서비스 패키지 자체는 계산 로직을 실행하지 않고, 하위 모듈들의 책임만 설명한다.
# 추천/로딩/분석 코드가 모두 페이지 밖으로 빠져 있다는 구조적 의도를 여기서 드러낸다.
