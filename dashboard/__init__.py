"""재난 대피소 추천 앱의 공통 패키지.

이 패키지는 설정, 콘텐츠, 서비스, 컴포넌트, 포맷터를 하나의 네임스페이스 아래에 모은다.
실행 로직은 없지만 ``dashboard`` import 경로를 고정해
페이지 파일이 상대경로 없이도 같은 구조를 바라보게 만드는 기준점 역할을 한다.
"""

# __init__.py 파일이 있으면 Python 은 이 폴더를 import 가능한 패키지로 인식한다.
# 패키지 진입점 파일이라 실행 코드는 두지 않고, 공통 네임스페이스 설명만 남긴다.
# 페이지와 서비스가 `dashboard.*` 경로를 일관되게 import 하도록 기준점 역할만 맡는다.
