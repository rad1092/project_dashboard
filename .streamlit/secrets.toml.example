# Copy this file to .streamlit/secrets.toml only when you need local overrides.

# Optional: override the repo-local preprocessing_data folder with another location.
# preprocessing_data_dir = "D:/my-preprocessing-data"

[app]
# Future public API placeholders only. Do not commit real keys.
realtime_alert_api_key = "your-public-api-key"
routing_api_key = "your-routing-api-key"
